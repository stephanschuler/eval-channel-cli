#!/bin/zsh

function() {
  local shellScriptPath=$1
  local shellScriptDir=$(dirname "$shellScriptPath")
  local phpScriptpath="$shellScriptDir/addition.php"

  . $shellScriptDir/../resources/call-with-back-channel

  alias addition="_callWithBackChannel $(realpath $phpScriptpath)"
} $0
